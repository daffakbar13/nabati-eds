import React from 'react'
import PageWhStockMutation from 'src/containers/PageWhStockMutation'

interface Props {}

export default function WhStockMutation(props: Props) {
  const {} = props

  return <PageWhStockMutation />
}
