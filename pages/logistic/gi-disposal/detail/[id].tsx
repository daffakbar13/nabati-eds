import React from 'react'
import PageGIDisposalDetail from 'src/containers/PageGIDisposal/detail'

interface props { }

export default function GIDisposalDetail(props: props) {
    const { } = props

    return <PageGIDisposalDetail />
}
