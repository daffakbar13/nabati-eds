import React from 'react'
import { PageAboutProps } from './types'

export default function PageAbout(props: PageAboutProps) {
    const { } = props

    return (
        <div>PageAbout</div>
    )
}
