import React from 'react'
import { PageDashboardProps } from './types'

export default function PageDashboard(props: PageDashboardProps) {
  const { } = props

  return (
        <div>PageDashboard</div>
  )
}
